// Minim Theme JavaScript
(function (Drupal) {
  'use strict';

  // Theme behaviors
  Drupal.behaviors.minim = {
    attach: function (context, settings) {
      // Theme initialization code goes here
      console.log('Minim theme loaded');
    }
  };

})(Drupal);
